"use strict";
function drawRectangle(options) {
    var a = options.width;
    var b = options.length;
    if (options.height) {
        var c = options.height;
    }
}
//Aufruf
drawRectangle({ width: 100, length: 50 });
