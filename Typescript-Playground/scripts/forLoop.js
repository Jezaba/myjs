"use strict";
let colors = ["Red", "Blue", "Green"];
for (let ix in colors) {
    console.log(colors[ix]);
}
for (let color of colors) {
    console.log(color);
}
let letters = "ABC";
for (let letter of letters) {
    console.log(letter);
}
